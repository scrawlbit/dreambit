<UserControl 
    x:Class="DreamBit.Extension.Windows.SceneInspect.TransformInspect"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:DreamBit.Extension.ViewModels"
    xmlns:input="clr-namespace:DreamBit.Extension.Controls.Input"
    xmlns:containers="clr-namespace:DreamBit.Extension.Controls.Containers"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:SceneInspectViewModel, IsDesignTimeCreatable=False}"
    d:DesignHeight="450" d:DesignWidth="800">

    <containers:ExpansionPanel Header="Transform">
        <Grid DataContext="{Binding Editor.Selection}">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- LABELS -->
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Position" />
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Scale" />
            <TextBlock Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Text="Rotation" />

            <TextBlock Grid.Column="1" Grid.Row="0" Text="X" Style="{StaticResource PropertyNameStyle}" />
            <TextBlock Grid.Column="1" Grid.Row="1" Text="X" Style="{StaticResource PropertyNameStyle}" />

            <TextBlock Grid.Column="3" Grid.Row="0" Text="Y" Style="{StaticResource PropertyNameStyle}" />
            <TextBlock Grid.Column="3" Grid.Row="1" Text="Y" Style="{StaticResource PropertyNameStyle}" />

            <!-- POSITION -->
            <!--<input:FloatBox Grid.Row="0" Grid.Column="2" Value="{Binding Transform.X, Mode=TwoWay}" StateManager="{StaticResource PositionStateManager}" />
            <input:FloatBox Grid.Row="0" Grid.Column="4" Value="{Binding Transform.Y, Mode=TwoWay}" StateManager="{StaticResource PositionStateManager}" />-->

            <!-- SCALE -->
            <!--<input:FloatBox Grid.Row="1" Grid.Column="2" Value="{Binding Transform.ScaleX, Mode=TwoWay}" StateManager="{StaticResource ScaleStateManager}" />
            <input:FloatBox Grid.Row="1" Grid.Column="4" Value="{Binding Transform.ScaleY, Mode=TwoWay}" StateManager="{StaticResource ScaleStateManager}" />-->

            <!-- ROTATION -->
            <!--<input:FloatBox Grid.Row="2" Grid.Column="2" Value="{Binding Transform.Rotation, Mode=TwoWay}" IsRotation="True" StateManager="{StaticResource RotationStateManager}" />-->
        </Grid>
    </containers:ExpansionPanel>
    
</UserControl>
